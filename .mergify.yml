pull_request_rules:
  - name: VM5 stale conflict timer proof
    conditions:
      - title = VM5_CONFLICT_TIMER_TRIGGER
      - -conflict
      - current-datetime >= 2026-09-13T02:31:30Z
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: vm5-conflict-timer.yml
              ref: main
