name: VM5 update-rule trusted CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  trusted-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Delay for controlled post-create mutation
        run: sleep 30
      - name: Pass
        run: echo VM5_UPDATE_RULE_TRUSTED_CI_PASS
