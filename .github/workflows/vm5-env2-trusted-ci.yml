name: VM5 env2 trusted CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  trusted-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Delay for controlled mutation
        run: sleep 30
      - run: echo VM5_ENV2_TRUSTED_CI_PASS
